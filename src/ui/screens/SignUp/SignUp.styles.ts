import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  logoContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  smallLogoContainer: {
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 15,
  },
  сontainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
